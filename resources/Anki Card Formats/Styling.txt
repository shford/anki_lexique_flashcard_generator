/* Make colors nicer - root is default and applies to all modes unless overriden (by .night_mode) */
:root {
  --text-color: #4a4a4a;
  --pron-color: #4a4a4a;
  --pos-color: #808080;
  --masc-color: #6755f2;
  --fem-color: #f25555;
}


.night_mode {
  --text-color: #fcfcfc;
  --pron-color: #fcfcfc;
  --pos-color: #808080;
  --masc-color: #80aaff;
  --fem-color: #ff8080;
}


.card {
  font-family: tahoma;
  font-size: 40px;
  text-align: center;
  color: var(--text-color);
  background-color: white;
}


/* Container for the POS + English Meaning */
.card-front {
  display: inline;
  white-space: normal;
  line-height: 1.2;
}


/* todo replace custom html tag gr w/ span class='gr' and .gr css */
gr {
  color: var(--pos-color);
  font-size: 20px;
}


/* Part of Speech */
.pos {
  font-style: italic;
  color: var(--pos-color);
  font-size: 20px;
  /* display: inline-block; */
  vertical-align: baseline;
  position: relative;
  margin-right: 1px;
}


/* English meaning (main word) */
.meaning {
  font-weight: 700;
  font-size: 40px;
  color: var(--text-color);
  /* display: inline-block; */
  vertical-align: baseline;
}


/* Noun Declension section on the back */
.noun-declension {
  font-size: 40px;
  color: var(--text-color);
  line-height: 1.3;
  margin-bottom: 12px;
}


/* Pronunciation */
.pronunciation {
  /*font-size: 28px;*/
  color: var(--pron-color);
  margin-bottom: 6px;
}


/* Sound button */
.sound {
  font-size: 20px;
}


/* Set colors for genre */
.masc {
  color: var(--masc-color);
}

.fem {
  color: var(--fem-color);
}
